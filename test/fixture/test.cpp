#include<iostream>
#include<vector>

int main() {
    std::vector<int> a;
    std::vec;
    int hoge;
    std::cin >> hoge;
    a.push_back(hoge);
    for (int i = 0; i < a.size(); i++) {
        a[i] = 0;
    }
}
